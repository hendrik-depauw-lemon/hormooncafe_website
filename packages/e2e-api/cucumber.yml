default:
    parallel: 1
    retry: 0
    publish: true
    requireModule:
        - ts-node/register
    require:
        - steps/**/*.ts
        - util/**/*.ts
    format:
        - '@cucumber/pretty-formatter'
        - usage-json:usage.json
        - progress
